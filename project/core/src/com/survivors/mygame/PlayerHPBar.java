package com.survivors.mygame;

public class PlayerHPBar {
    // Will be intangible
    // This will always be at an offset position from the
    //    Player’s character, and that will make it appear
    //    at the same position on the screen at all times,
    //    so it doesn’t need to consider any positions
}
