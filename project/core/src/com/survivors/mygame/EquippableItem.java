package com.survivors.mygame;

public class EquippableItem {
    /**
     * I think this should only need the String of the item's type and then
     * take it and compare it against all the valid equip item types
     * which will then assign specific values to variables in this class like
     * the item's sprite, or its attack rate and damage, or if it has an
     * on-hit/on-kill effect it triggers like ExplodesFromEnemy,
     * or its level (which influences stats), or its attack type and method of directing its attack, etc.
     *
     * @param equipName The word for the exact item information to load.
     */
    public EquippableItem(String equipName) {

    }
}
